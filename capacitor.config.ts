import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mock-test-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
